node_modules/
dist/
*.log
.env
apps/desktop/out/
*.tsbuildinfo
.agnt/
packages/server/reader.db*
apps/web/dist-demo/
site/public/demo/

site/.astro/
/scripts/demo-video/out/

# tman shims and run logs
/.tman/
/build
/build.ps1
/build.cmd
/test
/test.ps1
/test.cmd

# worktrack loop state is per-clone (lease holder carries an email); the
# workspace binding and the workflow manifest are shared, so they are committed.
.worktrack/*
!.worktrack/mcp.json
!.worktrack/templates.json
!.worktrack/slice-lint.json
