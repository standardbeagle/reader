{
  "name": "@reader/server",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "tsx src/index.ts",
    "test": "vitest run --passWithNoTests",
    "pretest": "pnpm --filter @reader/core build",
    "build": "tsc -b tsconfig.json"
  },
  "dependencies": {
    "@reader/core": "workspace:*",
    "fastify": "^5.2.0",
    "better-sqlite3": "^12.0.0",
    "p-limit": "^7.0.0"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vitest": "^3.0.0",
    "tsx": "^4.19.0",
    "@types/better-sqlite3": "^7.6.12",
    "@types/node": "^22.0.0"
  }
}
